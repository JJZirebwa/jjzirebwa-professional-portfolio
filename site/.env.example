# Optional Google Analytics 4 override for local builds.
# The site has a default GA4 measurement ID baked into the analytics component.
# Copy to site/.env.local only if you need to test a different GA4 web stream.
PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
